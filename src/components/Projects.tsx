import { motion } from "framer-motion";
import { projects } from "../data/portfolio";
export function Projects(){return <section id="projects" className="section"><h2>Projects / Case Studies</h2><div className="grid md:grid-cols-2 gap-4">{projects.map((p)=><motion.article key={p.title} whileHover={{y:-4}} initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="card"><h3>{p.title}</h3><p><strong>Problem:</strong> {p.problem}</p><p><strong>Solution:</strong> {p.solution}</p><p><strong>Impact:</strong> {p.impact}</p><div className="mt-2 flex flex-wrap gap-2">{p.technologies.map(t=><span key={t} className="badge">{t}</span>)}</div></motion.article>)}</div></section>;}
